import 'package:flutter/material.dart';

class CustomerTracking  extends StatelessWidget {
  const CustomerTracking ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Comentarios');
  }
}
