import 'package:flutter/material.dart';

class LogLesseeScreen extends StatelessWidget {
  const LogLesseeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Log lessee screen :D');
  }
}
