import 'package:flutter/material.dart';

class CasasScreen extends StatelessWidget {
   
  const CasasScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('CasasScreen'),
      ),
    );
  }
}